{
  "name": "io-auto-mode",
  "version": "0.1.0",
  "description": "Exec security classifier — hybrid static + LLM. Multi-platform: OpenClaw + Claude Code (WIP).",
  "type": "module",
  "scripts": {
    "benchmark": "tsx benchmarks/runner.ts",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "openclaw": "^2026.4.2"
  }
}
